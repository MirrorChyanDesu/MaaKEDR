version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    ignore:
      # Major TypeScript bumps need a manual upgrade (tooling / vuepress ecosystem).
      - dependency-name: typescript
        update-types: ["version-update:semver-major"]
